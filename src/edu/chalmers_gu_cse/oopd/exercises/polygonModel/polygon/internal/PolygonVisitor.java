package edu.chalmers_gu_cse.oopd.exercises.polygonModel.polygon.internal;

public interface PolygonVisitor<R> {

    // TODO 4: Visitor Pattern: Make all (components of) polygons visitable,
    //  and define here the components that a visitor, via pattern matching,
    //  needs to cover. Let R be the result type of the operation that the
    //  visitor represents.

    // TODO 8: For more challenge, instead implement the Acyclic Visitor Pattern.
}
